/** Inject the plugin card stylesheet into the browser bundle. */
const STYLE_ID = 'dsh-routed-subagent/styles'
const CSS = ".rsa-card {\n  list-style: none;\n  overflow: hidden;\n  border: 1px solid var(--dsw-alias-border-l2, #d8d8dc);\n  border-radius: 12px;\n  background: var(--dsw-alias-bg-layer-3, #fff);\n  color: var(--dsw-alias-label-primary, #202124);\n  transition: border-color .16s, background .16s;\n}\n\n.rsa-card:hover,\n.rsa-card[data-open='true'] {\n  border-color: var(--dsw-alias-label-dimmed, #a8a8ad);\n}\n\n.rsa-card[data-open='true'] {\n  background: var(--dsw-alias-bg-layer-2, #fafafa);\n}\n\n.rsa-card-header {\n  appearance: none;\n  width: 100%;\n  border: 0;\n  border-radius: 12px;\n  background: transparent;\n  color: inherit;\n  cursor: pointer;\n  display: flex;\n  align-items: center;\n  gap: 12px;\n  padding: 14px 16px;\n  text-align: left;\n  font: inherit;\n}\n\n.rsa-card-header:focus-visible,\n.rsa-button:focus-visible,\n.rsa-chip:focus-visible,\n.rsa-icon-button:focus-visible,\n.rsa-input:focus-visible,\n.rsa-textarea:focus-visible {\n  outline: 2px solid var(--dsw-alias-brand-primary, #4f6bed);\n  outline-offset: 2px;\n}\n\n.rsa-card-heading { min-width: 0; flex: 1; display: flex; flex-direction: column; gap: 4px; }\n.rsa-card-title { font-size: 15px; font-weight: 600; line-height: 1.4; }\n.rsa-card-description { color: var(--dsw-alias-label-tertiary, #6f7075); font-size: 13px; line-height: 1.5; }\n.rsa-unsaved { flex: none; border-radius: 999px; padding: 1px 8px; background: var(--dsw-alias-bg-module-platform, #ececf2); color: var(--dsw-alias-label-secondary, #4f5055); font-size: 11px; font-weight: 500; line-height: 17px; }\n.rsa-chevron { flex: none; color: var(--dsw-alias-label-tertiary, #6f7075); transition: transform .16s; }\n.rsa-card[data-open='true'] .rsa-chevron { transform: rotate(180deg); }\n\n.rsa-card-body { margin: 0 16px; padding: 14px 0 8px; border-top: 1px solid var(--dsw-alias-border-l2, #d8d8dc); }\n.rsa-toolbar { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 12px; }\n.rsa-section-copy { margin: 0; color: var(--dsw-alias-label-tertiary, #6f7075); font-size: 12px; line-height: 1.5; }\n.rsa-tier-list { display: flex; flex-direction: column; gap: 12px; }\n.rsa-empty { border: 1px dashed var(--dsw-alias-border-l2, #d8d8dc); border-radius: 10px; padding: 18px; text-align: center; color: var(--dsw-alias-label-tertiary, #6f7075); font-size: 13px; }\n\n.rsa-tier {\n  border: 1px solid var(--dsw-alias-border-l2, #d8d8dc);\n  border-radius: 10px;\n  background: var(--dsw-alias-bg-layer-3, #fff);\n  padding: 14px;\n}\n.rsa-tier-head { display: flex; align-items: flex-start; gap: 10px; margin-bottom: 12px; }\n.rsa-tier-index { min-width: 0; flex: 1; }\n.rsa-tier-label { display: block; margin-bottom: 6px; color: var(--dsw-alias-label-secondary, #4f5055); font-size: 11px; font-weight: 600; letter-spacing: .02em; text-transform: uppercase; }\n.rsa-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }\n.rsa-field { min-width: 0; display: flex; flex-direction: column; gap: 6px; }\n.rsa-field-wide { grid-column: 1 / -1; }\n.rsa-label { color: var(--dsw-alias-label-primary, #202124); font-size: 13px; font-weight: 500; line-height: 1.5; }\n.rsa-optional { color: var(--dsw-alias-label-tertiary, #6f7075); font-size: 11px; font-weight: 400; }\n.rsa-input,\n.rsa-textarea {\n  box-sizing: border-box;\n  width: 100%;\n  border: 1px solid var(--dsw-alias-border-l2, #d8d8dc);\n  border-radius: 8px;\n  background: var(--dsw-alias-bg-layer-2, #fafafa);\n  color: var(--dsw-alias-label-primary, #202124);\n  padding: 0 11px;\n  font: inherit;\n  font-size: 13px;\n}\n.rsa-input { height: 34px; }\n.rsa-textarea { min-height: 68px; padding-top: 8px; padding-bottom: 8px; resize: vertical; line-height: 1.5; }\n.rsa-input[aria-invalid='true'] { border-color: var(--dsw-alias-label-error, #d93025); }\n.rsa-input:disabled,\n.rsa-textarea:disabled,\n.rsa-button:disabled,\n.rsa-chip:disabled,\n.rsa-icon-button:disabled { cursor: default; opacity: .55; }\n.rsa-hint,\n.rsa-error { margin: 0; font-size: 12px; line-height: 1.45; }\n.rsa-hint { color: var(--dsw-alias-label-tertiary, #6f7075); }\n.rsa-error { color: var(--dsw-alias-label-error, #d93025); }\n\n.rsa-chips { display: flex; flex-wrap: wrap; gap: 7px; }\n.rsa-chip { appearance: none; border: 1px solid var(--dsw-alias-border-l2, #d8d8dc); border-radius: 999px; background: transparent; color: var(--dsw-alias-label-secondary, #4f5055); cursor: pointer; padding: 4px 10px; font: inherit; font-size: 12px; line-height: 1.4; }\n.rsa-chip[data-selected='true'] { border-color: var(--dsw-alias-brand-primary, #4f6bed); background: var(--dsw-alias-bg-module-platform, #ececf2); color: var(--dsw-alias-label-primary, #202124); }\n\n.rsa-button,\n.rsa-icon-button { appearance: none; border: 1px solid var(--dsw-alias-border-l2, #d8d8dc); border-radius: 8px; background: transparent; color: var(--dsw-alias-label-secondary, #4f5055); cursor: pointer; font: inherit; font-size: 13px; line-height: 1.5; }\n.rsa-button { padding: 5px 13px; }\n.rsa-button-primary { border-color: transparent; background: var(--dsw-alias-brand-primary, #4f6bed); color: var(--dsw-alias-label-onbrand, #fff); font-weight: 500; }\n.rsa-icon-button { flex: none; padding: 5px 9px; color: var(--dsw-alias-label-error, #d93025); }\n.rsa-button:hover:not(:disabled),\n.rsa-icon-button:hover:not(:disabled),\n.rsa-chip:hover:not(:disabled) { border-color: var(--dsw-alias-label-dimmed, #a8a8ad); }\n\n.rsa-footer { display: flex; align-items: center; gap: 8px; margin-top: 14px; padding-top: 12px; border-top: 1px solid var(--dsw-alias-border-l2, #d8d8dc); }\n.rsa-status { min-width: 0; flex: 1; margin: 0; color: var(--dsw-alias-label-secondary, #4f5055); font-size: 12px; line-height: 1.5; }\n.rsa-status[data-error='true'] { color: var(--dsw-alias-label-error, #d93025); }\n.rsa-readonly { margin: 0 0 12px; color: var(--dsw-alias-label-tertiary, #6f7075); font-size: 12px; }\n\n@media (max-width: 620px) {\n  .rsa-grid { grid-template-columns: minmax(0, 1fr); }\n  .rsa-field-wide { grid-column: auto; }\n  .rsa-toolbar { align-items: flex-start; flex-direction: column; }\n  .rsa-footer { align-items: stretch; flex-direction: column; }\n  .rsa-status { width: 100%; }\n  .rsa-footer .rsa-button { width: 100%; }\n}\n"
const SELECTOR_CSS = "\n.rsa-toolbar-actions { display: flex; align-items: center; gap: 8px; }\n.rsa-select { cursor: pointer; padding-right: 30px; text-overflow: ellipsis; }\n.rsa-select:disabled { cursor: default; }\n.rsa-catalog-notice { margin: -4px 0 12px; border-radius: 8px; padding: 8px 10px; background: var(--dsw-alias-bg-module-platform, #ececf2); color: var(--dsw-alias-label-secondary, #4f5055); font-size: 12px; line-height: 1.5; }\n.rsa-catalog-error { background: var(--dsw-alias-interactive-bg-hover-danger, #fce8e6); color: var(--dsw-alias-label-error, #d93025); }\n.rsa-catalog-warning { color: var(--dsw-alias-state-warn-label, #8a5a00); }\n@media (max-width: 620px) {\n  .rsa-toolbar-actions { width: 100%; }\n  .rsa-toolbar-actions .rsa-button { flex: 1; }\n}\n"

export function ensureStyles(): void {
  if (typeof document === 'undefined') return
  if (document.querySelector(`style[data-plugin-css="${STYLE_ID}"]`) !== null) return
  const tag = document.createElement('style')
  tag.dataset.plugin = 'dsh-routed-subagent'
  tag.dataset.pluginCss = STYLE_ID
  tag.textContent = CSS + SELECTOR_CSS
  document.head.appendChild(tag)
}
